Fixture format:
- `session`: Stage 1 structured session object subset used as Stage 2 input.
- `require_bill_ref`: Bool input for Stage 2.
- `expected`: Unit-style assertions for option count, distinct action families, and required constraint reflection.
