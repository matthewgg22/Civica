# ebt.ca.gov PoC findings

> Auto-generated by `fly/ebt-scraper/poc/probe.ts`. Each run **appends** a new
> section so you can compare day-over-day changes.


## Run @ 2026-05-23T01:47:13.298Z

Probe target list: root, cardholder, login.

### `root` — `https://www.ebt.ca.gov/`

- **Final URL:** `https://www.ebt.ca.gov/`
- **HTTP status:** `403`
- **HTML length:** 292 bytes

#### Anti-bot detection
- Cloudflare detected: **false**
- Captcha detected: **false**
- JS challenge detected: **false**

#### Cookies (Set-Cookie on initial GET)
- (none)

#### Form inputs (non-hidden)
- (none)

#### Hidden fields (CSRF / authenticity tokens)
- (none)
- CSRF detected: **false**

#### Notable response headers
- `strict-transport-security`: `max-age=31536000 ; includeSubDomains`

### `cardholder` — `https://www.ebt.ca.gov/cardholder/`

- **Final URL:** `https://www.ebt.ca.gov/cardholder/`
- **HTTP status:** `403`
- **HTML length:** 301 bytes

#### Anti-bot detection
- Cloudflare detected: **false**
- Captcha detected: **false**
- JS challenge detected: **false**

#### Cookies (Set-Cookie on initial GET)
- (none)

#### Form inputs (non-hidden)
- (none)

#### Hidden fields (CSRF / authenticity tokens)
- (none)
- CSRF detected: **false**

#### Notable response headers
- `strict-transport-security`: `max-age=31536000 ; includeSubDomains`

### `login` — `https://www.ebt.ca.gov/cardholder/login`

- **Final URL:** `https://www.ebt.ca.gov/cardholder/login`
- **HTTP status:** `403`
- **HTML length:** 308 bytes

#### Anti-bot detection
- Cloudflare detected: **false**
- Captcha detected: **false**
- JS challenge detected: **false**

#### Cookies (Set-Cookie on initial GET)
- (none)

#### Form inputs (non-hidden)
- (none)

#### Hidden fields (CSRF / authenticity tokens)
- (none)
- CSRF detected: **false**

#### Notable response headers
- `strict-transport-security`: `max-age=31536000 ; includeSubDomains`


### Run summary

- Cloudflare detected on any target: **false**
- Captcha detected on any target: **false**
- All targets returned HTTP 2xx: **false**
- Total unique cookies observed: 0

### Analyst notes (manual, not auto-generated)

**Headline finding:** all three targets returned **HTTP 403** with a tiny
(~300 byte) response and **no cookies set**. No Cloudflare challenge UI,
no captcha widget, no JS challenge. Single response header (HSTS).

**Interpretation:** the portal is fronted by a WAF (most likely Akamai
based on the bare 403 with no challenge HTML; alternative: AWS WAF +
IP-deny list). It is blocking at the edge based on either:
  1. Source IP reputation (this probe ran from a residential / dev IP
     pool that the WAF treats as automation), OR
  2. UA / TLS fingerprint pattern (despite using a real-Chromium UA
     string, the Playwright TLS handshake signature is detectable), OR
  3. Both.

It is **not** a Cloudflare bot-defense product (no `cf-ray`, no `cf-`
cookies, no challenge HTML). That rules out the easiest residential-proxy
+ captcha-solver path.

**Implications for plan §4.3 / Lane B:**

1. **Production scraper MUST use sjc Fly egress IPs OR a residential
   proxy.** This probe was run from a Mac on a residential ISP and still
   got 403'd — meaning either the WAF rejects most non-CA IPs, or it
   already has a deny list that covers common dev/data-center ranges.
   Need to re-run the probe **from a Fly machine in sjc** to know which.
2. **Risk table entry "ebt.ca.gov captcha / IP ban during scrape
   rollout" — Likelihood: High** stands. The WAF is already aggressive
   pre-scrape; the moment we ship aggregate volume, we should expect
   tightening.
3. **D4 (cookie handoff) still works** because the iOS WebView runs on
   the recipient's device with their residential IP — they won't hit the
   WAF for the login step. The cookie they ship to Civica is harvested
   client-side. We only need the WAF to admit the *server-side scrape*
   that uses the cookie. Risk: WAF may inspect cookie+IP affinity and
   reject when the same JSESSIONID appears from a different ASN.
4. **Next probe (Matthew):** run `probe-authed.ts` from your Mac (which
   the residential ISP allows the iOS app to use) and observe whether
   the *cookie-bearing* request succeeds even though the bare GET 403s.
   If yes → server-side scrape from sjc Fly probably also works once we
   bring the cookie. If no → we need to ship the scrape from a
   residential-proxy egress, which materially changes cost + ATO posture.

**What this run CAN'T tell us (still need probe-authed.ts):**

- Session cookie names (CMT-1) — login wasn't reached
- Session timeout (CMT-1) — couldn't even establish a session
- Transaction history depth (CMT-4) — same
- Remember-me cookie TTL (CMT-1) — same

**Re-run conditions:**

- After deploying skeleton to sjc Fly machine — re-run `probe.ts` from
  there to test whether sjc IPs are on the WAF allowlist.
- After Matthew runs `probe-authed.ts` once — compare the response
  shapes to see if cookie-bearing requests bypass the WAF.

---
