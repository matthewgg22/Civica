# MA moderate — asset test passes but one asset self-declared

Two MA assets, one Plaid-verified and one self-declared. Total still under MA
limit so the test passes.

**Why moderate:** asset_test_result=pass but verification is partial → moderate.
